# ===================================
# CONFIGURAÇÕES DO AMBIENTE
# ===================================
NODE_ENV=development

# ===================================
# BANCO DE DADOS PostgreSQL
# ===================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=seu_usuario
DB_PASS=sua_senha
DB_NAME=database_aisam

# ===================================
# JWT (Autenticação)
# ===================================
# IMPORTANTE: Alterar em produção para uma chave forte e única
JWT_SECRET=sua-chave-secreta-muito-forte-e-aleatoria

# ===================================
# CONFIGURAÇÕES DE E-MAIL (KINGHOST)
# ===================================
# Host SMTP da Kinghost
# Opção 1: smtp.kinghost.net (recomendado)
# Opção 2: mail.seudominio.com.br
MAIL_HOST=smtp.seudominio.com.br

# Porta SMTP
# 587 - TLS/STARTTLS (recomendado)
# 465 - SSL
MAIL_PORT=465

# Preencha com seu e-mail completo
# Exemplo: contato@seudominio.com.br ou noreply@seudominio.com.br
MAIL_USER=vagas@seudominio.com.br

# Preencha com a senha do e-mail
# A senha da conta de e-mail no painel Kinghost
MAIL_PASS=sua_senha_email

# E-mail que aparecerá como remetente
# Normalmente o mesmo que MAIL_USER
MAIL_FROM=vagas@seudominio.com.br

# ===================================
# URLs DO FRONTEND
# ===================================
# URL do frontend (para links em e-mails e CORS)
# Desenvolvimento: http://localhost:5175
# Produção: https://vagas.aisam.com.br
FRONTEND_URL=http://localhost:5175

# ===================================
# MONITORAMENTO (SENTRY)
# ===================================
# DSN do Sentry para monitoramento de erros (opcional)
# Obtenha em: https://sentry.io
# SENTRY_DSN=https://sua-chave@sentry.io/seu-projeto

# ===================================
# CONFIGURAÇÕES DE PRODUÇÃO
# ===================================
# URL da API em produção
# API_URL=https://api.vagas.aisam.com.br

# Nível de log (error, warn, info, debug)
# LOG_LEVEL=info

# ===================================
# OBSERVAÇÕES IMPORTANTES:
# ===================================
# 1. Este arquivo SERVE APENAS COMO EXEMPLO
# 2. Copie este arquivo para .env e preencha com seus dados reais
# 3. O arquivo .env NÃO deve ser commitado no Git (já está no .gitignore)
# 4. Depois de preencher MAIL_USER, MAIL_PASS e MAIL_FROM, reinicie o backend
# 5. Para testar se o e-mail está funcionando, use a rota POST /notificacoes/test
# 6. Em produção, use senhas fortes e mantenha as credenciais seguras
# 7. Para gerar um JWT_SECRET forte, use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# EXEMPLO DE COMANDO PARA GERAR JWT_SECRET:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
